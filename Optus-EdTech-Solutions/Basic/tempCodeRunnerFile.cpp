
    // if(head == nullptr){
    //     head = newNode;
    //     return;
    // }
    // Node *it = new Node();
    // while (it->next != nullptr)
    // {
    //     it = it->next;
    // }
    // it->next = newNode;